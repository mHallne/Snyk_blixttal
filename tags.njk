---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
    - tagList
  addAllPagesToCollections: true
layout: layouts/home.njk
renderData:
  title: Taggar med “{{ tag }}”
permalink: /tags/{{ tag }}/
---
<h1>Taggar med “{{ tag }}”</h1>

{% set postslist = collections[ tag ] %}
{% include "postslist.njk" %}

<p>Titta på <a href="{{ '/tags/' | url }}">alla tagar</a>.</p>
